package com.bank.frauddetection.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    /*
     * Add springdoc-openapi later if interactive Swagger UI is required.
     * The REST contract is currently represented by controller mappings and DTOs.
     */
}
